apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: prism-relay
  labels:
    app: prism-relay
spec:
  serviceName: prism-relay
  replicas: 1  # SQLite = single writer, do not scale horizontally
  podManagementPolicy: OrderedReady  # Ensures old pod terminates before new one starts
  updateStrategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: prism-relay
  template:
    metadata:
      labels:
        app: prism-relay
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
      containers:
        - name: relay
          image: ghcr.io/prismplural/prism-relay:latest
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: prism-relay-config
            - secretRef:
                name: prism-relay-secrets
                optional: true
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            # Memory limit budgets for the worst-case in-flight upload mix:
            # 8 snapshot PUTs × 150 MB = 1.2 GB, plus 32 media uploads × 10 MB
            # = 320 MB, plus baseline + headroom. Lower the concurrency knobs
            # (SNAPSHOT_UPLOAD_CONCURRENCY, MEDIA_UPLOAD_CONCURRENCY) in the
            # ConfigMap if you want to fit on a smaller node.
            limits:
              memory: "2Gi"
              cpu: "1000m"
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: prism-relay-data
