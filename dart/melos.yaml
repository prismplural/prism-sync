name: prism_sync_workspace
packages:
  - packages/**

command:
  bootstrap:
    usePubspecOverrides: true

scripts:
  test:
    description: Run every Dart/Flutter package with a test directory.
    run: melos exec --dir-exists=test -- flutter test
